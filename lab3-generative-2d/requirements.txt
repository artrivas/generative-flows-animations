# Core
torch==2.12.1
numpy==2.5.0

# Plotting & animation
matplotlib==3.11.0
Pillow==12.3.0          # backend para gif export
ffmpeg-python==0.2.0    # wrapper para video mp4 via ffmpeg (currently unused in src/scripts, kept pinned for parity with the venv)
imageio-ffmpeg==0.6.0   # bundled ffmpeg binary used by animation scripts

# Scientific
scipy==1.18.0           # para KDE, estadísticas de validación
einops==0.8.2           # manipulación de tensores limpia

# Config management
omegaconf==2.3.1        # used by scripts/main.py's --config loader for YAML configs

# Progress / logging
tqdm==4.68.3

# Optional: aceleración ODE
# torchdiffeq>=0.2.3    # descomentar si se usa dopri5 en el integrador
